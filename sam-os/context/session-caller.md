# Session Caller — Agent Initialization Template

Copy the block below as your **first message to a new opencode agent session** running GLM 5.2 (or any capable model). It loads the full system context, agent definitions, and sets the current task.

---

**[COPY FROM HERE]**

/sam-os init

Load the following files in order to initialize with full context:

**1. System Identity & Strategy**
- `sam-os/context/vantage-agent-init.md` — State of Transfer: strategic mandate, partnerships, North Star, current ops status, ecosystem map, weekly rhythm, command menu.

**2. Operating Protocol**
- `sam-os/AGENTS.md` — Sam-OS identity, session protocol, precedence chain, core rules, command menu, available agents, boot sequence.
- `sam-os/context/session-protocol.md` — Mandatory 6-step /orient procedure.

**3. Available Agents (load as needed)**
- `sam-os/agents/lead-magnet-agent.md` — Lead magnet generation agent (for content / lead magnet tasks).

**4. Tool Configuration**
- `opencode.json` — Provider config: GLM 5.2 via Zenmux (1M context, 131K output).

**5. Session Directives**
- `/orient` — Run full situation assessment before any task.
- State the North Star: **50 paying members at R399/mo in 90 days.**
- Report: current phase, what's built vs pending, blockers, next action.

**6. Current Task (set this per session)**
- **[REPLACE WITH YOUR TASK]** — e.g. "Generate the 10 AI Prompts lead magnet HTML", "Prepare Tuesday playbook draft", "Draft the Rentia workshop curriculum outline"

**[END COPY]**
